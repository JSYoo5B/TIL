#!/bin/sh

# Add problem-solving common tools by adding path
export PATH=$PATH:$(pwd)/../.tool/:$(pwd)/.tool/

# Add python path to use auto tester tools
export PYTHONPATH=${PYTHONPATH}:$(pwd)/../.tool/:$(pwd)/.tool/

# Set current path as ALGOSPOT_HOME for auto tools
export ALGOSPOT_HOME=$(pwd)
